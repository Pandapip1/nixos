From a879cf709a92643047046348066802357e061ea9 Mon Sep 17 00:00:00 2001
From: Gavin John <gavinnjohn@gmail.com>
Date: Fri, 17 Jul 2026 16:35:37 -0400
Subject: [PATCH 2/2] drm/edid: add xreal air non-desktop quirk in SBS

This uses the newly-added EDID_QUIRK_REQUIRE_RESOLUTION to mark Xreal Air
glasses as non-desktop when they are in stereoscopic 3D mode.

Signed-off-by: Gavin John <gavinnjohn@gmail.com>
---
 drivers/gpu/drm/drm_edid.c | 4 ++++
 1 file changed, 4 insertions(+)

diff --git a/drivers/gpu/drm/drm_edid.c b/drivers/gpu/drm/drm_edid.c
index 07878d2622be..2f17796ed2d0 100644
--- a/drivers/gpu/drm/drm_edid.c
+++ b/drivers/gpu/drm/drm_edid.c
@@ -284,6 +284,10 @@ static const struct edid_quirk {
 	EDID_QUIRK('S', 'V', 'R', 0x1019, BIT(EDID_QUIRK_NON_DESKTOP)),
 	EDID_QUIRK('A', 'U', 'O', 0x1111, BIT(EDID_QUIRK_NON_DESKTOP)),
 
+	/* Xreal Air; exposes 3840x1080 mode in SBS */
+	EDID_QUIRK_REQUIRE_RESOLUTION('M', 'R', 'G', 0x3132, 3840, 1080,
+				      BIT(EDID_QUIRK_NON_DESKTOP)),
+
 	/* LQ116M1JW10 displays noise when 8 bpc, but display fine as 6 bpc */
 	EDID_QUIRK('S', 'H', 'P', 0x154c, BIT(EDID_QUIRK_FORCE_6BPC)),
 
-- 
2.54.0

