name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  nix-flake-check:
    runs-on: codeberg-medium

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Nix
        uses: https://github.com/cachix/install-nix-action@v31.9.0
        with:
          GITHUB_TOKEN: ""

      - name: Run nix flake check
        run: nix flake check
